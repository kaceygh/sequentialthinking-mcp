{
  "name": "sequentialthinking-mcp-dcdeploy",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "supergateway": "1.0.0"
  },
  "scripts": {
    "start": "supergateway --host 0.0.0.0 --stdio \"npx @modelcontextprotocol/server-sequential-thinking\" --port $PORT --healthPath /health --ssePath /sse"
  }
}
